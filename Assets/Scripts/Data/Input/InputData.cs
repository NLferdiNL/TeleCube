using UnityEngine;

public class InputData : MonoBehaviour {
    public bool click, leftArrow, rightArrow, upArrow, downArrow = false;

    public bool mouseMove = false;

    public Vector3 mousePos = Vector3.zero;
}
